# Experiments

Small, throwaway prototype scripts that test a specific detection idea against sample
vendored C code. Not production code — keep each one simple and scoped to one question.

When adding an experiment, give it its own subfolder with a short note (in this file or
a per-experiment README) covering:

- What question it was trying to answer
- What it did (approach)
- What the result was
